#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i;//for es igual que while pero inicializo, decremento e incremento dentro del paréntesis
    for (i=0;i<10;i++){
        printf("%d\n", (i+1));
    }
    return 0;//
}
